# ---- MineRL 依存と整合が取れている最小セット ----
gym==0.23.1
numpy==1.24.4
torch==2.2.2            # CPU wheel（公式 PyPI）
stable-baselines3==1.7.0
cleanrl==0.4.6
networkx<3
# MineRL はサイズが大きい & gym pin があり依存衝突するので
# Dockerfile で --no-deps で個別インストールする
